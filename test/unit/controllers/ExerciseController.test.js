"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ExerciseController');

describe('controllers:ExerciseController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
