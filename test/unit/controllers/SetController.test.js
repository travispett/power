"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/SetController');

describe('controllers:SetController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
