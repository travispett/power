"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/WorkoutController');

describe('controllers:WorkoutController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
