"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Set');
const Fixture = require('../../fixtures/SetFixture');

describe('models:Set', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
